# SESSION NOTES TEMPLATE — RETIRED

**Status:** Retired 2026-06-13. This file previously documented `sitl_v8.js`, the Node.js `.docx` generator for SITL session notes.

The workflow no longer produces a `.docx`. Session notes are now authored directly as the canonical **markdown note** in `01-Sessions/` during Convo 1. For content and section structure, see `SESSION_NOTES_SECTION_BREAKDOWN.md`.

The old `sitl_v8.js` generator and the `sitl-v8-docx` skill are no longer part of the pipeline.
